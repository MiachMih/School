import React from 'react'

export default function Options() {
  return(
    <div class="Options">
      <button>Add</button>
      <button>Find</button>
      <button>View All</button>
      <button>Remove</button>
    </div>

  );
}
